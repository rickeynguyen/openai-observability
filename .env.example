# Copy to .env and fill in your key (use a low-limit key just for monitoring!)
OPENAI_API_KEY=sk-...
# Region label (cosmetic only)
PROBE_REGION=us-west-1
# Probe interval in seconds (keep modest to control spend)
PROBE_INTERVAL_SEC=60
# Model to probe (small + cost efficient)
OPENAI_MODEL=gpt-4o-mini
# Request timeout (ms)
HTTP_TIMEOUT_MS=12000
# Enable SQLite persistence (1 to enable)
ENABLE_DB=0
# Optional custom DB file path
DB_FILE=./data.db

# Probe configuration
# Comma-separated probe keys (default: safe set). Examples: chat,embeddings,models
# PROBE_ENDPOINTS=chat,embeddings,models,moderations,files
# Probe mode: roundrobin (default) or all
# PROBE_MODE=roundrobin
# Include heavy probes like images generation (costly). 1 to enable.
# PROBE_HEAVY=0
# Optional model overrides
# OPENAI_EMBEDDINGS_MODEL=text-embedding-3-small
# OPENAI_MODERATION_MODEL=omni-moderation-latest

# ===== AI-centric observability (logs + chat) =====
# Enable AI features and logs (requires ENABLE_DB=1)
# When ENABLE_AI=1, the UI shows the Chat tab with logs, simulator, and embeddings indexing.
ENABLE_AI=0

# Protects /logs/sim/* and /ai/index/auto/* routes. The UI defaults to 'secret'.
INGEST_API_KEY=secret

# Log simulator options (insert synthetic logs periodically)
# Auto-start the simulator on server boot (1 to enable)
LOG_SIM_ENABLED=0
# Interval between synthetic log batches (ms)
LOG_SIM_INTERVAL_MS=30000

# Embeddings indexing interval for auto-index (ms)
EMB_INDEX_INTERVAL_MS=45000
